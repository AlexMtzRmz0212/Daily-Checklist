OPENROUTER_API_KEY=your_openrouter_api_key_here
MODEL=your_model_name_here

# Optional global Notion fallback. Primary config is per-user (encrypted, set in the app's
# Settings). These are only used when a user hasn't configured their own token.
NOTION_TOKEN=
NOTION_VERSION=2022-06-28